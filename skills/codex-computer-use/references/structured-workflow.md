# Structured workflow wrapper

When multi-agent orchestration or a workflow needs structured pass/fail output.

## Wrapper label

`gpt-5.6-sol:ui-verify`

## Schema

Use workflow `schema` or structured MCP output when the parent must parse pass/fail, evidence, and defects.

## Scope

Verification only — scope fixes separately via [codex-implementation](../codex-implementation/SKILL.md) (`--profile implement`) when explicitly requested.
