# General engineering (sol + high reasoning, workspace edits).
# Usage: codex exec --profile engineer --ephemeral ...
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
approval_policy = "never"
sandbox_mode = "workspace-write"
service_tier = "fast"
