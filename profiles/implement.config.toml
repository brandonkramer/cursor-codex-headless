# Headless implementation (bulk/mechanical edits).
# Usage: codex exec --profile implement --ephemeral ...
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
approval_policy = "never"
sandbox_mode = "workspace-write"
service_tier = "fast"
