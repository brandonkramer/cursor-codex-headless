# Cheap exploratory headless passes (re-run on implement/review before shipping).
# Usage: codex exec --profile probe --ephemeral ...
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
approval_policy = "never"
sandbox_mode = "read-only"
service_tier = "fast"
