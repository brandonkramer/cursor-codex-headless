# Headless read-only review (PR, diff, architecture).
# Usage: codex exec --profile review --ephemeral ...
model = "gpt-5.6-sol"
model_reasoning_effort = "ultra"
approval_policy = "never"
sandbox_mode = "read-only"
service_tier = "fast"
